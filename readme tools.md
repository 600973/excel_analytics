# 📚 Инструкция по использованию утилит проекта

## 🌳 Tree Generator - Генератор структуры проекта

### Описание
Создает визуальное представление структуры директорий проекта в различных форматах.

### Основные возможности
- ✅ 5 форматов вывода: tree, list, json, markdown, html
- ✅ Автоматическое сохранение рядом со скриптом
- ✅ Умные правила игнорирования
- ✅ Поддержка Git (только отслеживаемые файлы)
- ✅ Статистика проекта
- ✅ Красивые иконки для типов файлов

### Быстрый старт

```bash
# Базовое использование
python tree_generator.py .

# Markdown формат
python tree_generator.py . --format markdown

# С размерами файлов
python tree_generator.py . --sizes

# Только Git файлы
python tree_generator.py . --git-only

# Только вывод в консоль
python tree_generator.py . --console

# Указать свой путь сохранения
python tree_generator.py . --output=/path/to/file.txt
```

### Параметры

| Параметр | Короткий | Описание |
|----------|----------|----------|
| `path` | - | Путь к проекту (по умолчанию: `.`) |
| `--format` | `-f` | Формат: tree, list, json, markdown |
| `--output` | `-o` | Путь для сохранения |
| `--console` | `-c` | Только вывод в консоль |
| `--sizes` | `-s` | Включить размеры файлов |
| `--git-only` | `-g` | Только Git файлы |
| `--ignore` | `-i` | Дополнительные исключения |

### Примеры вывода

#### Tree формат
```
📁 myproject/
├── 📁 src/
│   ├── 🐍 main.py
│   └── 🐍 utils.py
├── 📁 tests/
│   └── 🐍 test_main.py
└── 📝 README.md
```

---

## 📦 Save All In One - Объединитель файлов

### Описание
Собирает все текстовые файлы проекта в один файл для передачи в LLM (Claude, ChatGPT и т.д.)

### Основные возможности
- ✅ 5 форматов вывода: txt, markdown, json, xml, html
- ✅ Поддержка 50+ типов файлов
- ✅ Красивая статистика проекта
- ✅ Оглавление и навигация
- ✅ Подсветка синтаксиса в HTML/Markdown
- ✅ Контроль размера файлов
- ✅ Автоматическое сохранение рядом со скриптом

### Быстрый старт

```bash
# Базовое использование (txt формат)
python save_all_in_one.py .

# Markdown формат (лучше для LLM)
python save_all_in_one.py . --format markdown

# HTML формат (красиво для просмотра)
python save_all_in_one.py . --format html

# Все форматы сразу
python save_all_in_one.py . --all-formats

# Только определенные папки
python save_all_in_one.py . --folders src bots prompts

# Только корневые файлы
python save_all_in_one.py . --root-only

# Только Python и JS файлы
python save_all_in_one.py . --extensions .py,.js
```

### Параметры

| Параметр | Короткий | Описание |
|----------|----------|----------|
| `path` | - | Путь к проекту (по умолчанию: `.`) |
| `--format` | `-f` | Формат: txt, markdown, json, xml, html |
| `--all-formats` | `-a` | Создать все форматы сразу |
| `--folders` | - | Список папок для обработки |
| `--root-only` | `-r` | Только корневые файлы |
| `--extensions` | `-e` | Дополнительные расширения |
| `--ignore` | `-i` | Дополнительные исключения |
| `--output` | `-o` | Папка для сохранения |
| `--max-size` | `-m` | Макс. размер файла в MB (по умолчанию: 10) |
| `--no-stats` | - | Без статистики |

### Поддерживаемые типы файлов

#### Код
Python, JavaScript, TypeScript, Java, C++, C#, Go, Rust, PHP, Ruby, Swift, Kotlin

#### Web
HTML, CSS, SCSS, Vue, Svelte, React

#### Конфигурация
JSON, YAML, TOML, INI, ENV

#### Документация
Markdown, TXT, RST

#### Данные
CSV, TSV, XML, SQL

---

## 🎯 Практические примеры

### Сценарий 1: Подготовка проекта для LLM

```bash
# Генерируем структуру проекта
python tree_generator.py . --format markdown

# Объединяем весь код в один файл
python save_all_in_one.py . --format markdown --folders src bots prompts

# Результат: 2 файла готовы для загрузки в Claude/ChatGPT
# 1. structure_projectname_timestamp.md - структура
# 2. combined_projectname_timestamp.md - весь код
```

### Сценарий 2: Анализ только Python кода

```bash
# Структура только Python файлов
python tree_generator.py . --ignore="*.js,*.css,*.html"

# Объединение только Python файлов
python save_all_in_one.py . --extensions .py --format markdown
```

### Сценарий 3: Git-only режим

```bash
# Только отслеживаемые Git файлы
python tree_generator.py . --git-only --format markdown

# Объединение с учетом Git
python save_all_in_one.py . --format markdown
# (файлы из .gitignore автоматически игнорируются)
```

### Сценарий 4: Экспорт в HTML для презентации

```bash
# Красивая HTML структура
python tree_generator.py . --format markdown --sizes

# Красивый HTML со всем кодом
python save_all_in_one.py . --format html

# Откройте HTML файл в браузере!
```

### Сценарий 5: Полный экспорт проекта

```bash
# Все форматы для дерева
python tree_generator.py . --format markdown
python tree_generator.py . --format json
python tree_generator.py . --format list

# Все форматы для кода (одной командой)
python save_all_in_one.py . --all-formats

# Результат: 8 файлов со всей информацией о проекте
```

---

## 💡 Советы и рекомендации

### Для работы с LLM

1. **Используйте Markdown формат** - он лучше всего понимается LLM моделями
2. **Сначала покажите структуру** - загрузите дерево проекта
3. **Потом загрузите код** - файл с объединенным кодом
4. **Разбивайте большие проекты** - используйте `--folders` для обработки по частям

### Оптимизация размера

```bash
# Ограничить размер файлов до 5MB
python save_all_in_one.py . --max-size 5

# Игнорировать большие папки
python save_all_in_one.py . --ignore="tests,docs,examples"

# Только самое важное
python save_all_in_one.py . --folders src --extensions .py
```

### Именование файлов

Файлы автоматически сохраняются с временной меткой:
- `structure_myproject_20241107_143052.txt`
- `combined_myproject_20241107_143052.md`

Это позволяет отслеживать версии и не перезаписывать старые файлы.

---

## 🔧 Настройка

### Добавление своих расширений

```bash
# В tree_generator.py добавить в DEFAULT_IGNORE_PATTERNS
python tree_generator.py . --ignore="*.bak,*.old,temp*"

# В save_all_in_one.py указать свои типы файлов
python save_all_in_one.py . --extensions .custom,.special
```

### Изменение игнорирования по умолчанию

Отредактируйте в файлах константы:
- `DEFAULT_IGNORE_PATTERNS` - что игнорировать
- `DEFAULT_TEXT_EXTENSIONS` - какие файлы обрабатывать

---

## ❓ FAQ

**Q: Файлы сохраняются не там где нужно?**
A: Используйте параметр `--output` для указания своего пути

**Q: Как объединить только определенные файлы?**
A: Используйте `--folders` и `--extensions` для фильтрации

**Q: Файл слишком большой для LLM?**
A: Используйте `--max-size` или обработайте проект по частям с `--folders`

**Q: Нужно ли устанавливать зависимости?**
A: Нет! Оба скрипта используют только стандартную библиотеку Python

**Q: Можно ли использовать с другими ЯП?**
A: Да! Скрипты работают с любыми текстовыми файлами

---

## 📄 Лицензия

Свободное использование для любых целей.

## 🤝 Контакты

Если нужна помощь - просто запустите скрипт с флагом `--help`:

```bash
python tree_generator.py --help
python save_all_in_one.py --help
```

---

**Версия:** 2.0  
**Дата:** 2024-11-07